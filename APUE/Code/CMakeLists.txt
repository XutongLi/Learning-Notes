cmake_minimum_required(VERSION 3.10)
PROJECT(apue)
SET(CMAKE_CXX_STANDARD 11)

INCLUDE_DIRECTORIES(include)
AUX_SOURCE_DIRECTORY(C3 DIR_SRCS)

SET(TEST_MATH
    ${DIR_SRCS}
    )

ADD_EXECUTABLE(${PROJECT_NAME} C3/lseek.cpp)

TARGET_LINK_LIBRARIES(${PROJECT_NAME} m)


